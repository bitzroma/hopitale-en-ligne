<form method="post" action="{{ path('user_supprimer', {'id': user.id}) }}" onsubmit="return confirm('Vous etes sure de bien vouloir supprimer votre compte ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}"><br>
    <button style="background-color: red">Supprimé</button>
</form>
